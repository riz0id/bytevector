-- | TODO
--
-- @since 1.0.0
module Control.Comonad.Store.Unlifted
  (
  )
where

import GHC.Types

--------------------------------------------------------------------------------

-- newtype Store# :: TYPE r -> TYPE r' -> TYPE UnliftedRep where
--   Store# :: (# a -> b, a #) -> Store# a b
